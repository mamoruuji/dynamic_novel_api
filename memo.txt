// sqlboilerのモデル自動生成
sqlboiler psql

// buf、protoファイルからconnect-goのファイルを生成
buf lint
buf generate

// 動作確認
go run ./cmd/main.go

// // 別ターミナルから実行
grpcurl \
-protoset <(buf build -o -) -plaintext \
-d '{}' \
localhost:8080 api.proto.dynamic.v1.DynamicService/GetDynamics
