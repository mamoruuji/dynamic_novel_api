// sqlboilerのモデル自動生成
sqlboiler psql

// buf、protoファイルからconnect-goのファイルを生成
buf lint
buf generate

// 動作確認
go run ./cmd/server/main.go

// // 別ターミナルから実行
curl \
--header "Content-Type: application/json" \
--data '{}' \
http://localhost:8080/proto.dynamic.v1.DynamicService/GetDynamics

curl \
--header "Content-Type: application/json" \
--data '{}' \
http://127.0.0.1:8080/proto.dynamic.v1.DynamicService/GetDynamics


curl \
--header "Content-Type: application/json" \
--data '{}' \
http://localhost:8080/v1/dynamics

grpcurl \
-protoset <(buf build -o -) -plaintext \
-d '{}' \
localhost:8080 proto.dynamic.v1.DynamicService/GetDynamics

ドキュメント生成
protoc \
--doc_out=./doc \
--doc_opt=markdown,dynamic_api_doc.md \
proto/dynamic/v1/*.proto \
--plugin=protoc-gen-doc=$GOPATH/bin/protoc-gen-doc

// パスの重複削除
export PATH="$PATH:$(go env GOPATH)/bin"
echo $PATH | tr : \\n | sort
export PATH="$(printf %s "$PATH" | awk -v RS=: -v ORS=: '!arr[$0]++')"

